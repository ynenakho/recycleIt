capsule {
  id (peacedevs.recycleit)
  version (0.1.0)
  format (3)
  targets {
    target (bixby-mobile-en-US)
  }
  capsule-imports {
    import (viv.contact) {version (2.5125.7)}
    import (viv.geo) {version-from (viv.contact)}
  }
  permissions {
    user-profile-access
  }
}
