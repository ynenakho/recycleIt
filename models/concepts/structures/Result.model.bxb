structure (Result) {
  description (result after all API calls and calculations)
  property (item) {
    type (Item)
    min (Required) max (One)
  }
  property (info) {
    type (Info)
    min (Required) max (One)
  }
}