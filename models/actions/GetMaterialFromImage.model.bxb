action (GetMaterialFromImage) {
  type(Search)
  description (Grab image data from API service)
  collect {
    input (image) {
      type (Image)
      min (Required) max (One)
    }
  }
  output (Material)
}
