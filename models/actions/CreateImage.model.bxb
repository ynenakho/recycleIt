action (CreateImage) {
  type(Constructor)
  description (Constructor of image data)
  collect {
    input (imageDataConcept) {
      type (ImageDataConcept)
      min (Required) max (One)
      default-init {
        intent {
          goal: FetchImageAction
        }
      }
    }
  }
  output (Image)
}
