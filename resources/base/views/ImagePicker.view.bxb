input-view {  
  match: Image (this) {
    to-input: SelectImage
  }

  message {
    template (Gallery) {
      speech (What would you like to recycle?)
    }
  }

  render {
    image-picker (this) {
      size (Medium)
    }
  }
}